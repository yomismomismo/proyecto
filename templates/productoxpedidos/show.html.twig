<!DOCTYPE html>

<title>Productoxpedidos</title>

{% block body %}
    <h1>Productoxpedidos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productoxpedido.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ productoxpedido.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('productoxpedidos_index') }}">back to list</a>

    <a href="{{ path('productoxpedidos_edit', {'id': productoxpedido.id}) }}">edit</a>

    {{ include('productoxpedidos/_delete_form.html.twig') }}
{% endblock %}
